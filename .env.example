# Neon Postgres connection string (Dashboard -> Connect -> copy the pooled URL).
# Must include ?sslmode=require
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Same key the portfolio uses (Google AI Studio). Free tier is fine.
GOOGLE_GENERATIVE_AI_API_KEY=
